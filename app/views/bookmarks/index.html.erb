<div class="container">
  <h1>My Bookmarks</h1>
    <ul>
    	<% @bookmarks.each do |bookmark| %>
    	<li><%= link_to bookmark.title, bookmark %></li>
        <%= bookmark.hashtags.each do |hashtag| %>
          <li><%= link_to hashtag.title, tag_path(hashtag) %></li>
      	<% end %>
      <% end %>
    </ul>

  <%= link_to 'Create Bookmark', new_bookmark_path %>
</div>